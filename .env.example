# Optional Tau chat agent and interview transcription
OPENAI_API_KEY=
MODEL_NAME=

# Optional official-platform credentials. Provide them through your shell or the
# Streamlit one-time input; never commit real values.
LINKEDIN_ACCESS_TOKEN=
X_BEARER_TOKEN=
